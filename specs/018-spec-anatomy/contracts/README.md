# Contracts

**N/A** — Esta feature es una sección educativa estática dentro del sitio web existente. No expone interfaces externas (APIs, CLIs, librerías). Toda la interacción es interna al navegador (click events, expand/colapsar, cambio de tabs).

Ver la [sección UI en quickstart.md](../quickstart.md) para los puntos de integración con el HTML/CSS/JS existente del sitio.
